const input = [
  [1, 1, 0],
  [1, 1, 0],
  [0, 0, 1],
];

const node = new Map();
let result = 0;

// input.forEach((element, index) => {
//   element.splice(index, 1);
//   node.set(index, element);
// });

console.log(input);
